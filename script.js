const DATA=window.PORTFOLIO_DATA||{projects:[],home:[]};const views=[...document.querySelectorAll('.view')];let currentView='home',previousView='project',heroIndex=0,currentLightImages=[],lightIndex=0;function show(id){views.forEach(v=>v.classList.toggle('active',v.id===id));currentView=id;document.getElementById(id)?.scrollTo(0,0)}document.querySelectorAll('[data-route]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.route)));const heroImage=document.getElementById('heroImage'),heroCount=document.getElementById('heroCount');function renderHero(){if(!DATA.home.length){heroImage.removeAttribute('src');heroCount.textContent='';return}const item=DATA.home[heroIndex];heroImage.style.opacity='0';setTimeout(()=>{heroImage.src=item.src;document.getElementById('heroTitle').textContent=item.title||'HOME';heroCount.textContent=`${String(heroIndex+1).padStart(2,'0')} / ${String(DATA.home.length).padStart(2,'0')}`;heroImage.style.opacity='1'},120)}document.getElementById('prevHero').addEventListener('click',()=>{if(!DATA.home.length)return;heroIndex=(heroIndex-1+DATA.home.length)%DATA.home.length;renderHero()});document.getElementById('nextHero').addEventListener('click',()=>{if(!DATA.home.length)return;heroIndex=(heroIndex+1)%DATA.home.length;renderHero()});const grouped={};DATA.projects.forEach(p=>(grouped[p.group||'Projects']??=[]).push(p));const list=document.getElementById('projectList');Object.entries(grouped).forEach(([group,projects])=>{const wrap=document.createElement('div');wrap.className='project-group';wrap.innerHTML=`<h3>${group}</h3>`;projects.forEach(project=>{const button=document.createElement('button');button.textContent=project.title;button.addEventListener('click',()=>openProject(project.slug,'project'));wrap.appendChild(button)});list.appendChild(wrap)});function openProject(slug,from='project'){const project=DATA.projects.find(p=>p.slug===slug);if(!project)return;previousView=from;document.getElementById('detailTitle').textContent=project.title;const gallery=document.getElementById('detailGallery');gallery.innerHTML='';project.images.forEach((src,index)=>{const holder=document.createElement('div');holder.className='detail-image';const image=document.createElement('img');image.src=src;image.loading='lazy';image.alt=`${project.title} ${index+1}`;image.addEventListener('click',()=>openLightbox(project.images,index));holder.appendChild(image);gallery.appendChild(holder)});show('detail')}document.getElementById('backBtn').addEventListener('click',()=>show(previousView));const feed=document.getElementById('projectFeed');DATA.projects.filter(p=>p.images?.length).slice(0,20).forEach((project,index)=>{const figure=document.createElement('figure');figure.className='feed-item';const image=document.createElement('img');image.src=project.images[(index*3)%project.images.length];image.loading='lazy';image.alt=project.title;figure.addEventListener('click',()=>openProject(project.slug,'project'));figure.appendChild(image);feed.appendChild(figure)});const lightbox=document.getElementById('lightbox'),lightboxStage=document.getElementById('lightboxStage');function openLightbox(images,index){currentLightImages=images;lightIndex=index;lightbox.classList.add('open');lightbox.setAttribute('aria-hidden','false');renderLight()}function renderLight(){document.getElementById('lightImage').src=currentLightImages[lightIndex];document.getElementById('lightCounter').textContent=`${lightIndex+1} / ${currentLightImages.length}`}function closeLightbox(){lightbox.classList.remove('open');lightbox.setAttribute('aria-hidden','true');document.getElementById('lightImage').removeAttribute('src')}document.getElementById('closeLightbox').addEventListener('click',e=>{e.stopPropagation();closeLightbox()});lightbox.addEventListener('click',e=>{if(e.target===lightbox||e.target===lightboxStage)closeLightbox()});document.getElementById('lightPrev').addEventListener('click',e=>{e.stopPropagation();lightIndex=(lightIndex-1+currentLightImages.length)%currentLightImages.length;renderLight()});document.getElementById('lightNext').addEventListener('click',e=>{e.stopPropagation();lightIndex=(lightIndex+1)%currentLightImages.length;renderLight()});document.getElementById('lightImage').addEventListener('click',e=>e.stopPropagation());document.addEventListener('keydown',e=>{if(lightbox.classList.contains('open')){if(e.key==='Escape')closeLightbox();if(e.key==='ArrowLeft')document.getElementById('lightPrev').click();if(e.key==='ArrowRight')document.getElementById('lightNext').click();return}if(currentView==='home'){if(e.key==='ArrowLeft')document.getElementById('prevHero').click();if(e.key==='ArrowRight')document.getElementById('nextHero').click()}if(e.key==='Escape'&&currentView==='detail')show(previousView)});renderHero();