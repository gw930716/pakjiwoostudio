# Pakjiwoo Studio V7

Upload the extracted contents of this folder to the root of the GitHub repository.

## Home slideshow
Use `images/home/001.jpg`, `002.jpg`, `003.jpg` and edit the `home` list in `data.js` to control order.

## Project order
The order of each project's `images` array in `data.js` controls website order.

## V7 fixes
- Working X close button
- ESC closes lightbox
- Clicking white space closes lightbox
- No automatic crop
- Separate home slideshow folder
